[project]
name = "wonderpy"
version = "0.2.0"
description = "Python API for working with Wonder Workshop robots"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bleak>=2.1.1",
    "mock>=5.2.0",
    "svgpathtools>=1.7.2",
]
